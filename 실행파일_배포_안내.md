# 다른 Windows PC에서 실행하기

MokpoYouthPolicyLauncher.exe는 서비스 소스가 내장된 Docker 실행 도우미입니다. Python, Node.js, MySQL을 따로 설치하거나 프로젝트 폴더를 함께 복사할 필요는 없습니다.

## 최초 한 번

1. Docker Desktop을 설치하고 실행합니다.
2. MokpoYouthPolicyLauncher.exe를 원하는 위치에 저장합니다.
3. 실행파일을 더블클릭하고 서비스 시작을 누릅니다.

첫 실행에는 서비스 파일이 %LOCALAPPDATA%\\MokpoYouthPolicy에 준비되고 Docker 이미지가 다운로드되므로 시간이 걸립니다. 이후에는 훨씬 빠르게 시작됩니다.

## 처음 생성되는 설정

런처는 .env가 없을 때 .env.example을 복사하고 MySQL 비밀번호, 세션 키, VAPID 키를 자동 생성합니다. 다음 기능을 쓰려면 .env 설정 열기에서 각 값을 한 번 입력해야 합니다.

- 카카오 로그인: KAKAO_REST_API_KEY, 필요 시 KAKAO_CLIENT_SECRET
- Gemini 상담: GEMINI_API_KEY
- 카카오 로그인 Redirect URI: 기본 포트가 8080이면 http://localhost:8080/auth/kakao/callback 을 카카오 개발자 콘솔에 등록

.env에는 비밀번호·API 키가 있으므로 다른 사람이나 GitHub에 공유하지 않습니다.

## 데이터 보관

정책 DB와 수집 데이터는 Docker 볼륨에 저장됩니다. 런처의 서비스 중지는 컨테이너만 멈추며 데이터를 삭제하지 않습니다. 데이터를 지우는 Docker 명령은 런처에서 제공하지 않습니다.
